const header=document.querySelector('#siteHeader'),menu=document.querySelector('#menuButton'),nav=document.querySelector('#mainNav'),modal=document.querySelector('#formModal'),form=document.querySelector('#demoForm'),message=document.querySelector('#formMessage'),title=document.querySelector('#modalTitle');
addEventListener('scroll',()=>header.classList.toggle('scrolled',scrollY>20));menu.addEventListener('click',()=>{nav.classList.toggle('open');menu.setAttribute('aria-expanded',nav.classList.contains('open'))});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>observer.observe(e));
document.querySelectorAll('.filter').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(b=>b.classList.remove('active'));button.classList.add('active');document.querySelectorAll('.talent-profile').forEach(card=>card.classList.toggle('hidden',button.dataset.filter!=='all'&&card.dataset.category!==button.dataset.filter))}));
function configureForm(mode){message.textContent='';message.style.color='';form.reset();const role=form.querySelector('select');let password=form.querySelector('[name=password]');if(mode==='login'){title.textContent='Masuk ke Kalyana';role.closest('label').hidden=true;if(!password){password=document.createElement('input');password.type='password';password.name='password';password.required=true;password.placeholder='Kata sandi';const label=document.createElement('label');label.dataset.password='true';label.append('Kata sandi',password);role.closest('label').before(label)}form.querySelector('[name=name]').closest('label').hidden=true;form.querySelector('[name=name]').required=false;form.querySelector('button[type=submit]').textContent='Masuk';form.dataset.mode='login'}else{title.textContent=mode==='talent'?'Gabung sebagai Talent':'Daftar sebagai UMKM';role.closest('label').hidden=false;role.value=mode==='talent'?'Talent Mahasiswa':'UMKM';if(password)password.closest('label').remove();form.querySelector('[name=name]').closest('label').hidden=false;form.querySelector('[name=name]').required=true;form.querySelector('button[type=submit]').textContent='Kirim Pendaftaran';form.dataset.mode='register'}}
document.querySelectorAll('[data-open]').forEach(b=>b.addEventListener('click',()=>{configureForm(b.dataset.open);modal.classList.add('open');modal.setAttribute('aria-hidden','false');setTimeout(()=>modal.querySelector('input:not([hidden])').focus(),50)}));document.querySelectorAll('[data-close]').forEach(b=>b.addEventListener('click',()=>{modal.classList.remove('open');modal.setAttribute('aria-hidden','true')}));addEventListener('keydown',e=>{if(e.key==='Escape')modal.classList.remove('open')});
form.addEventListener('submit',async e=>{e.preventDefault();message.textContent='Memproses...';const data=Object.fromEntries(new FormData(form)),isLogin=form.dataset.mode==='login';try{const response=await fetch(isLogin?'/api/auth/login':'/api/registrations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)}),result=await response.json();if(!response.ok)throw new Error(result.error||'Permintaan gagal.');if(isLogin){localStorage.setItem('kalyana_token',result.token);localStorage.setItem('kalyana_user',JSON.stringify(result.user));message.textContent='Login berhasil. Selamat datang, '+result.user.name+'!'}else message.textContent=result.message;form.reset()}catch(error){message.textContent=error.message;message.style.color='#a82828'}});
